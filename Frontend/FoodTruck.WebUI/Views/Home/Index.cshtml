@model IEnumerable<FoodDto>

@{
    ViewData["Title"] = "Home Page";
}
@* <script src="~/lib/microsoft/signalr/dist/browser/signalr.min.js"></script>
@section Scripts
{
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <script type="text/javascript">
                            $(document).ready(() => {
                                var connection = new signalR.HubConnectionBuilder().withUrl("https://localhost:7180/TruckHub").build();
                                $("#constatus").text(connection.state);
                                connection.start().then(() => {
                                    $("#constatus").text(connection.state);
                                    setInterval(() => {
                                        connection.invoke("SendTruckCount");
                                    }, 1000);
                                }).catch((err) => { console.log(err) });

                                connection.on("ReceiveTruckCount", (value) => {
                                    $("#truckcount").text(value);
                                });
                            });
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </div>
}


<div class="row">
    <div class="col-md-8 offset-2">
        <div class="alert alert-info">Bağlantının Durumu: <strong id="constatus"></strong></div>
    </div>
    <div class="col-md-8 offset-2">
        <div class="alert alert-danger">Araba Sayısı: <strong id="truckcount"></strong></div>
    </div>
</div> *@
<form>
    <div class="container row">
        @foreach (var item in Model)
        {
            <div class="col-12 col-md-6 col-lg-3">
                <div class="p-0 my-3 border rounded">
                    <div class="card">
                        <h3 class="card-title text-dark-50 text-center py-2">@item.Name</h3>
                        <img src="@item.ImageUrl" height="180" class="card-img-top" />
                        <div class="card-body">
                            <div class="d-flex justify-content-between py-2">
                                <span class="text-danger" style="font-size:20px;">@string.Format("{0:c}", @item.Price)</span>

                            </div>
                            <p class="card-text">@Html.Raw(item.Description)</p>
                            <div class="row">
                                <div class="col">
                                    <a asp-action="FoodDetails" asp-route-foodId="@item.FoodId" class="btn btn-success form-control">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</form>